---
title: Skeleton
description: Serves as a placeholder to indicate loading content.
preview: true
links:
  api: /docs/ui/skeleton#api-reference
---

<ComponentTabs name="skeleton-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://clippkit.com/r/skeleton"
```

### Manual

<Steps>

  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="skeleton" />

  </Step>

  <Step>

    Define the shimmer keyframes and CSS variables inside your `globals.css` file using the `@theme inline` block.

    ```css title="globals.css"
    @import "tailwindcss";

    @theme inline {
    /* other css vars */
    // [!code highlight:20]
      --animate-skeleton-shimmer: skeleton-shimmer 3s infinite ease-out;
      --animate-skeleton-gradient: skeleton-gradient 3s infinite ease-out;

      @keyframes skeleton-shimmer {
        0% {
          transform: translateX(-100%);
        }
        100% {
          transform: translateX(200%);
        }
      }
      @keyframes skeleton-gradient {
        0%,
        100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }
    }
    ```

  </Step>
  
  <Step>Update the import paths to match your project setup.</Step>

</Steps>

## Usage

Import all parts and piece them together.

```tsx
import { Skeleton } from "@/components/ui/skeleton";
```

```tsx
<Skeleton />
```

## Examples

### Pulse

<ComponentTabs name="skeleton-demo" />

### Shimmer

<ComponentTabs name="skeleton-shimmer" />

### Gradient

<ComponentTabs name="skeleton-gradient" />

## API Reference

### Root

The main container component for handling skeleton props.

<AutoTypeTable path="./types/docs/skeleton.ts" name="RootProps" />
